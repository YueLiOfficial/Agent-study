# 2026-08-11 工作日志

## 第 3 步完成，第 4 步开讲（Streamlit 网页界面）
- 用户完成 chat3.py 多轮对话 ✅（模型能记住对话历史）。
- 第 3 课追加「九、第 4 步：Streamlit 界面（chat4.py）」：
  - 核心概念：Streamlit 每次交互整脚本重跑 → messages 必须放 st.session_state（跨重跑持久，类比成员变量/static）。
  - chat4.py 完整代码（st.chat_input / st.chat_message 气泡 / st.empty() 占位符流式刷新）、运行方式（pip install streamlit + streamlit run）、常见坑（失忆/端口占用）、打卡标准。
- 学习进度.md 更新为「第 4 步（Streamlit 网页界面 chat4.py）」。

## 第 4 步完成 + 模型换 qwen3.5:4b + 第 5 步开讲（FastAPI 服务化）
- 用户完成 chat4.py Streamlit 界面 ✅ → 勾选「Streamlit 页面开发」。
- 模型再次更换：deepseek-r1:1.5b → **qwen3.5:4b**。讲义全文替换模型名，deepseek-r1 相关「思考过程」说明改为通用的「推理模型说明」。
- 第 3 课追加「十、第 5 步：FastAPI 服务化（chat_api.py）」：
  - 架构：任意客户端（curl/Qt）→ POST /chat → FastAPI(8000) → Ollama → qwen3.5:4b。
  - 核心概念：API 无状态（历史由客户端维护，与 LLM 无状态同一哲学）；FastAPI 装饰器路由 + pydantic 请求体模型 + /docs 自动文档 + uvicorn 启动。
  - 打卡标准 = 里程碑 1 最终验收；完成后下一步：模块 2 语言进阶 或 云端 API 对比。
- 学习进度.md 更新为「第 5 步（FastAPI 服务化 chat_api.py）」。

## 第 5 步：用户用 ollama SDK 实现 + 讲解流式升级
- 用户接口改用官方 **ollama SDK**（client.chat 替代裸 requests），非流式版已跑通。SDK 与 requests 等价，代码更简洁。
- 讲解两层流式：① ollama SDK stream=True → 生成器；② FastAPI StreamingResponse → 边生成边发给客户端；附客户端接收代码（requests stream=True + iter_lines）。
- 第 3 课「十」追加 8. 节：SDK 版代码、流式升级完整代码、客户端接收示例。
- 学习进度.md 更新为「第 5 步进行中：SDK 非流式 ✅，流式升级中」。

## 里程碑 1 完工 🎉 + Prompt 工程确认
- 用户完成流式版 /chat（两层流式）→ **里程碑 1 全部完成**：chat1 基础调用 / chat2 流式 / chat3 多轮 / chat4 Streamlit / chat5 FastAPI+SDK+流式。
- 勾选：FastAPI 接口开发、Ollama SDK 调用；简历项目储备「本地聊天机器人」标记完成。
- 用户询问 Prompt 工程是否在计划中 → 确认已有（模块 3 · 第 10 周：Zero-shot/Few-shot/CoT/结构化输出）；在计划中补充「Prompt 贯穿全程」提示，并注明可在里程碑 1 后提前插入「Prompt 微课」。
- 下一步候选：Prompt 工程微课（用户对 prompt 表现出兴趣）或 模块 2 语言进阶。

## Prompt 微课开讲（用户选 A）
- 新建 第4课-提示词工程（微课）.md：5 大技巧（角色设定/指令清晰/Few-shot/CoT/结构化输出）+ prompt_test.py 对比脚本（ollama SDK）+ 心法总结 + 打卡任务。
- 重点强调：结构化输出 JSON = 程序集成关键（json.loads 直接解析，类比 C++ 反序列化）；已用过 system 人格 = 实践起步。
- 学习进度.md 更新为「Prompt 微课进行中」。待用户跑实验打卡。
