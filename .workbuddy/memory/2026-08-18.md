# 2026-08-18 学习日志

## 进度整理（无新学习）
- 8/16–17 周末休息，未学习；今天仅做进度同步整理。
- 学习进度.md：新增「进度整理：2026-08-18」记录，其余内容（第 9、10 课 ✅、第 11 课进行中）确认无误。
- MEMORY.md 修正两处跨电脑同步导致的旧信息：
  - 本机工作区路径 → C:\Users\YueLi\Desktop\Agent-study
  - MySQL 环境实测配置 → mysql:latest 26.7.0，端口 13306→3306，root 密码 root123456，操作方式 docker exec -it
- 下一步：第 11 课 Numpy/Pandas 练习 4 题（Numpy 统计 / DataFrame 筛选 / 分组聚合 / read_sql 读 alarms 表）→ 模块 2 收官。

## 作业检查（第 11 课练习）
- 用户提交第 11 课作业代码，用 WorkBuddy 隔离 venv 安装 numpy/pandas/pymysql/sqlalchemy 后跑通验证：
  - Numpy 统计 ✅、DataFrame 筛选 ✅（E-101/E-103）、read_sql+groupby ✅（1号86.85、2号93.00）
  - 问题：`df.groupby('type').agg(...)` 缺 print()；`conn` 建而不用；`engin` 拼写错。作业通过，待用户改 print 后确认收官。
- **环境澄清（用户重要约定）**：所有 AI 项目用 conda aipy（D:\miniconda3\envs\aipy），已含全部所需包；WorkBuddy 隔离 venv 仅用于助手侧验证，未污染 aipy。已写入 MEMORY.md。

## 学习计划 v2.0 → v2.1（依据尚硅谷大纲精选补充）
- 用户提供《20260513学科手册-大模型.pdf》（尚硅谷，82 页/10 阶段/24 项目），pypdf 提取全文存 .workbuddy/tmp/pdf_text.txt。
- 用户选择「精选补充」策略，更新学习计划.md 为 v2.1：
  - 新增「模块 2 补充 · 工程基础」第 10 周：Linux/Shell + Docker（主线 30→31 周）
  - 第 9 周并入：MySQL 开窗函数、数据可视化（Matplotlib/Seaborn）
  - 模块 5 里程碑 2 增强：MinerU/OCR 图文混排解析 + 智能切片 + RAGAS 评估 + Graph RAG（对标尚硅谷「掌柜智库」）
  - 模块 6 补充：NL2SQL 案例（衔接 MySQL/PyMySQL，对标「掌柜问数」）
  - 强化学习专题深化：RLHF 三阶段（SFT→RM→PPO）+ DPO/GRPO 复刻 R1
  - 模块 3-7 周次顺延；差异说明新增 v2.1 条目
- 学习进度.md：路线版本更新为 v2.1，新增「后续安排」（第 11 课完成后进工程基础周）。

## 数据可视化教学（第 11 课扩展，v2.1）
- 讲 Matplotlib 四基础图（折线/柱状/直方/散点，subplots 2×2）+ Pandas 一行流（df.plot.bar）+ Seaborn boxplot；演示图 3 张存 阶段1-Python基础/可视化演示*.png。
- 关键坑：Matplotlib 默认无中文字体须 rcParams 设置；sns.set_theme 会重置字体配置（先 set_theme 再设字体）。
- 用户练习 ✅：四宫格 + plot.bar + boxplot；发现问题：①缺 plt.show()/savefig 图不显示 ②Series.plot.bar(x=...) 的 x 参数多余（index 即 device_id，pandas 3.0.5 静默忽略）。

## MySQL 开窗函数教学（v2.1 新增，第 9 周收官）
- 三类窗口函数：聚合类 COUNT/SUM/AVG OVER(PARTITION BY)（保留行+附加组统计，对照 GROUP BY）、排名类 ROW_NUMBER/RANK/DENSE_RANK（并列跳号区别）、取值类 LAG/LEAD。
- 工业应用：LAG 温度突变量 ≥3℃ = 设备异常预警雏形。
- 布置 2 道练习：①ROW_NUMBER 取每设备最高温度报警 ②LAG 找温度突升 ≥3℃ 报警。
- **模块 2 · 语言进阶收官 ✅**；学习进度.md/MEMORY.md 已更新（下一站：模块 2 补充·工程基础 Linux/Shell+Docker，第 10 周）。

## 开窗函数练习检查（晚间）
- 用户提交 2 题答案，全部正确 ✅：
  - ① ROW_NUMBER 子查询 + WHERE rn=1 取每设备最高温度报警（Top-N per group 经典模式）
  - ② LAG 温度突变量 + WHERE temp_diff>=3 找突升报警；PARTITION BY 防跨组比较、NULL 自动过滤第一条，均正确
- 小瑕疵：语句结尾双分号 `;;`（无害）。模块 2 收官确认完成。

## 讲义同步补录（用户指出：内容需落盘讲义）
- 用户批评：教学内容未同步进讲义，违背"换电脑无缝衔接"原则。已补录：
  - 第9课-MySQL数据库基础.md：新增「七、开窗函数」章节（三类窗口函数/排名三兄弟区别/LAG 预警/练习 5-6 题），章节重编号，预告更新为已完成状态
  - 第11课-Numpy与Pandas数据处理.md：新增「六、数据可视化」章节（中文字体坑/四基础图/Pandas 一行流/Seaborn/选图速查/练习 5-7 题），修正 MySQL 端口为 13306，预告更新为 v2.1 版
  - 可视化演示脚本.py 从 tmp 复制到讲义目录（演示图 3 张此前已存）
- 教训：**每次教学新内容都要同步更新讲义**（并入协作约定）。

## 学习计划 LangChain/LangGraph 细化（用户指出）
- 用户问"大纲里怎么没有 LangChain/LangGraph"——实际有但写得太薄（第 18 周"LangChain 核心概念"、第 23 周"LangGraph 基础"各一句话），扫文件看不出框架课的分量。
- 已细化：第 18 周→LangChain（上）Model I/O/Chains/Memory+RAG 原理；第 19 周→LangChain（下）Retrieval/Agents+向量库检索全家桶；第 23 周→LangGraph 图状思维（State/Node/Edge/持久化/流式）+智能体+案例。对应尚硅谷第六阶段框架课。
