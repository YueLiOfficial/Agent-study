# 2026-08-10 工作日志

## 学习模式调整：项目先行
- 用户决定跳过顺序学习，直接开始里程碑 1（聊天机器人），Python 基础语法以后穿插补。
- 用户现状：能读懂 Python 代码但写不熟练（C++ 工程师）。
- 新建 第3课-聊天机器人搭建.md：核心概念（LLM 无状态 + messages 消息流）、系统架构、第 1 步任务（装 Ollama + 拉 qwen2.5:3b + 跑通 chat1.py）、C++ 视角对照、验证标准与常见坑。
- 环境检查：本机未装 Ollama → 安装是用户今晚第 1 个任务。
- 学习进度.md 更新为「里程碑 1 · 聊天机器人 · 第 1 步」。

## 里程碑 1 第 1 步完成 + requests 讲解 + FastAPI 入纲
- 用户完成第 1 步：Ollama 命令行对话 ✅、chat1.py 打印自我介绍 ✅、能解释 LLM 无状态。
- 用户 Python 没学过 requests → 本轮逐行讲解 chat1.py（requests.post / json= 自动序列化 / resp.json() / C++ 对照）。
- 应要求把 FastAPI 加入学习大纲：模块 1 新增第 6 周「聊天机器人服务化：FastAPI 入门」，为终极项目 Qt 客户端 ↔ 本地 LLM 的 HTTP 通信打基础；模块 5 的 FastAPI 高并发内容保留为深化。
- 学习进度.md：勾选 requests 调用 HTTP API；已掌握清单拆分「FastAPI 接口开发」与「Docker 部署」。

## 模型纠正 + 第 2 步（流式输出）
- 用户纠正：实际使用的模型是 deepseek-r1:1.5b（非讲义默认 qwen2.5:3b）→ 讲义/进度/记忆全部改为 deepseek-r1:1.5b。
- 第 3 课追加「七、第 2 步：流式输出」：两个 stream 开关（body "stream":true + requests stream=True）、NDJSON 逐行格式、chat2.py 完整代码、R1 推理模型思考过程说明、今晚任务。
- 学习进度.md 更新为「第 2 步（流式输出 chat2.py）」。

## 第 2 步完成，第 3 步开讲（多轮对话）
- 用户完成 chat2.py 打字机效果 ✅ → 勾选「大模型 API 调用（流式输出）」。
- 第 3 课追加「八、第 3 步：多轮对话（chat3.py）」：messages 作为记忆容器、函数封装 chat()、while 主循环、assistant 回答必须存回历史、R1 思考过程正则过滤（re.sub）、上下文窗口痛点伏笔（衔接 RAG）。
- 学习进度.md 更新为「第 3 步（多轮对话 chat3.py）」。
